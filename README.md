Developer of the website has implemented some security changes to the backend code of the PasteBin application but it is still possible to preform RCE. Try to find the bug that allows you to do so and then leak the contents of the flag.txt file!

Difficulty: Normal

Hint: Variable $exec_ is 100% safe so there is no need to replace any characters, right?

@deadoverflow